# Инструкция по развертыванию на Render.com

## Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на [github.com](https://github.com)
2. Нажмите кнопку "New" для создания нового репозитория
3. Назовите его `yogurt-cost-calculator`
4. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть в проекте)
5. Нажмите "Create repository"

## Шаг 2: Загрузите код на GitHub

Выполните следующие команды в терминале из папки проекта:

```bash
cd /Users/mihailmirosnicenko/yogurt-cost-calculator

# Добавьте remote URL (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/yogurt-cost-calculator.git

# Отправьте код на GitHub
git branch -M main
git push -u origin main
```

## Шаг 3: Разверните на Render.com

### 3.1 Создайте аккаунт
1. Перейдите на [render.com](https://render.com)
2. Зарегистрируйтесь или войдите через GitHub (рекомендуется)

### 3.2 Создайте новый Web Service
1. В панели Render нажмите **"New +"** → **"Web Service"**
2. Подключите ваш GitHub репозиторий `yogurt-cost-calculator`
3. Настройте параметры:
   - **Name**: `yogurt-cost-calculator` (или любое другое имя)
   - **Runtime**: `Python`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `gunicorn app:app`
   - **Plan**: `Free` (выберите бесплатный тариф)

### 3.3 Переменные окружения (опционально)
Если нужно, добавьте переменные окружения:
- `PYTHON_VERSION`: `3.11.0`

### 3.4 Деплой
1. Нажмите **"Create Web Service"**
2. Render автоматически начнет деплой
3. Процесс займет 3-5 минут

## Шаг 4: Получите URL приложения

После успешного деплоя:
1. Render покажет URL вашего приложения
2. Он будет выглядеть так: `https://yogurt-cost-calculator.onrender.com`
3. Откройте этот URL в браузере

## Важные замечания

### О бесплатном плане Render:
- ✅ Бесплатно навсегда
- ⚠️ Приложение "засыпает" после 15 минут неактивности
- ⚠️ Первый запрос после пробуждения может занять 30-60 секунд
- ✅ После пробуждения работает быстро
- ✅ 750 часов работы в месяц (достаточно для личного использования)

### Автоматическое обновление:
- При каждом push в GitHub, Render автоматически обновит приложение
- Вы можете отключить автоматический деплой в настройках

### Хранение данных:
- ⚠️ На бесплатном плане данные могут быть потеряны при перезапуске
- Для надежного хранения рассмотрите:
  - Использование внешней БД (PostgreSQL на Render)
  - Хранение файлов в облаке (AWS S3, Google Cloud Storage)

## Альтернативные способы деплоя

### Быстрый деплой через кнопку
Добавьте эту кнопку в README.md:

```markdown
[![Deploy to Render](https://render.com/images/deploy-to-render-button.svg)](https://render.com/deploy)
```

### Используйте render.yaml
Файл `render.yaml` уже создан в проекте. Render автоматически его обнаружит и применит настройки.

## Troubleshooting

### Приложение не запускается
1. Проверьте логи в Render Dashboard
2. Убедитесь, что все файлы загружены на GitHub
3. Проверьте, что requirements.txt содержит gunicorn

### 502 Bad Gateway
- Подождите 1-2 минуты, приложение может еще запускаться
- Проверьте логи на наличие ошибок

### Данные не сохраняются
- Это нормально для бесплатного плана
- Используйте PostgreSQL addon в Render для постоянного хранения

## Поддержка

Если возникли проблемы:
1. Проверьте логи в Render Dashboard
2. Убедитесь, что приложение работает локально
3. Проверьте документацию Render: https://render.com/docs

## Следующие шаги

После успешного развертывания:
1. 📱 Добавьте приложение в закладки
2. 🔗 Поделитесь ссылкой с командой
3. 🎨 Настройте custom домен (платная функция)
4. 💾 Настройте базу данных для постоянного хранения

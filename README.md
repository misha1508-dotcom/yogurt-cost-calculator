# Калькулятор себестоимости йогурта

Веб-сервис конструктор для расчета себестоимости и юнит-экономики производства йогурта на реутери.

## Возможности

- **Группировка переменных по категориям:**
  - 🥛 Сырье (молоко, закваска, инулин)
  - 📦 Упаковка (бутылки ПЭТ, платинка, лотки, коробки)
  - 🚚 Логистика (транспорт)
  - 💰 Налоги
  - 👷 Работа
  - 🏢 Аренда
  - 📋 Другие расходы

- **Расчет показателей:**
  - Общая себестоимость партии
  - Себестоимость единицы продукции
  - Прибыль с единицы
  - Маржинальность (%)
  - Разбивка затрат по категориям

- **Сохранение конфигураций:**
  - Создание именованных версий настроек
  - Загрузка сохраненных конфигураций
  - Удаление конфигураций

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Запустите приложение:
```bash
python app.py
```

3. Откройте браузер по адресу:
```
http://localhost:5000
```

## Использование

### Добавление позиций

1. В каждой категории есть предзаполненные примеры
2. Нажмите "+ Добавить" чтобы добавить новую позицию
3. Заполните название, цену и количество
4. Сумма рассчитывается автоматически

### Расчет себестоимости

1. Укажите размер партии (количество единиц продукции)
2. Укажите цену продажи за единицу
3. Заполните все необходимые позиции затрат
4. Нажмите "Рассчитать себестоимость"

### Сохранение конфигурации

1. Введите название конфигурации в поле вверху страницы
2. Нажмите "Сохранить конфигурацию"
3. Конфигурация сохранится с текущими настройками

### Загрузка конфигурации

1. Нажмите "Загрузить конфигурацию"
2. Выберите нужную конфигурацию из списка
3. Нажмите "Загрузить"

## Структура проекта

```
yogurt-cost-calculator/
├── app.py                 # Основной файл приложения Flask
├── requirements.txt       # Зависимости Python
├── README.md             # Документация
├── data/                 # Папка для хранения данных
│   └── configurations.json  # Сохраненные конфигурации
├── static/               # Статические файлы
│   ├── css/
│   │   └── style.css    # Стили
│   └── js/
│       └── app.js       # JavaScript логика
└── templates/            # HTML шаблоны
    └── index.html       # Главная страница
```

## API Endpoints

- `GET /` - Главная страница
- `GET /api/configurations` - Получить все конфигурации
- `GET /api/configuration/<id>` - Получить конфигурацию по ID
- `POST /api/configuration` - Сохранить новую конфигурацию
- `DELETE /api/configuration/<id>` - Удалить конфигурацию
- `POST /api/calculate` - Рассчитать себестоимость

## Технологии

- **Backend:** Flask (Python)
- **Frontend:** HTML, CSS, JavaScript
- **Хранение данных:** JSON файлы

## Лицензия

MIT
